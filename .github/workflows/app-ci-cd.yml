name: app-ci-cd

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # Warmup "Hello, pipeline" artifact (keep this forever)
      - name: Warmup artifact (hello-1809)
        run: |
          echo "run $GITHUB_RUN_ID for $GITHUB_SHA" > hello-1809.txt
      - uses: actions/upload-artifact@v4
        with:
          name: hello-1809
          path: hello-1809.txt

      # Setup Python and run tests
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install pytest

      - name: Run tests
        run: pytest -q

      # Validate Docker build
      - name: Validate Docker build
        run: |
          SHA=$(git rev-parse --short HEAD)
          docker build -t app:${SHA}-1809 .

  deploy:
    needs: build-test
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    env:
      AWS_REGION: us-east-1
      ECR_REPO: ci-cd-ecs-1809
      ECS_CLUSTER: ecs-1809-cluster
      ECS_SERVICE: ecs-1809-service
    steps:
      - uses: actions/checkout@v4

      - name: Configure AWS credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::865414894725:role/github-oidc-ecs-deploy
          aws-region: ${{ env.AWS_REGION }}

      - name: Login to Amazon ECR
        id: ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Build & Push Docker image
        run: |
          set -euo pipefail
          SHA=$(git rev-parse --short HEAD)
          ACCOUNT_ID=$(aws sts get-caller-identity --query Account --output text)
          REGISTRY="${ACCOUNT_ID}.dkr.ecr.${{ env.AWS_REGION }}.amazonaws.com"
          IMAGE="${REGISTRY}/${{ env.ECR_REPO }}:${SHA}-1809"
          docker build -t "$IMAGE" .
          docker push "$IMAGE"
          echo "IMAGE=$IMAGE" >> $GITHUB_ENV

      - name: Render new ECS task definition
        run: |
          sed "s|IMAGE_URI|${IMAGE}|g" ecs-taskdef-template.json > taskdef.json

      - name: Deploy to Amazon ECS
        uses: aws-actions/amazon-ecs-deploy-task-definition@v2
        with:
          task-definition: taskdef.json
          service: ${{ env.ECS_SERVICE }}
          cluster: ${{ env.ECS_CLUSTER }}
          wait-for-service-stability: true
